# 密钥 —— 绝不入库
.env
.env.*
!.env.example

node_modules/
dist/
*.log

# 生图管线中间产物（体积大且可重跑）
scratch/
.sprite-work/

# 自检用的合成素材，由 npm run sprite:fixture 重新生成
assets/character/fixture/

# Electron 构建产物
out/

# 文档站构建产物（由 zensical build 生成，内容与 docs/ 一一对应，可重新生成）。
# wrangler.toml 指向 site/，误入库会让发布上传上一次的陈旧页面。
/site/

# Live2D 预处理产物（可由 scripts/prepare-model.ts 重新生成）
assets/model/

# Python 运行期产物与缓存
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.uv-cache/
/.tmp/

# pytest --basetemp 落在仓库内时留下的目录；Windows 的 ACL 让它们事后删不掉
.pytest-tmp-*/
/test-artifacts/

# Windows 的空设备名 nul 在 Git Bash 等 POSIX shell 里不被识别，`cmd 2>nul`
# 会当成重定向到当前目录的同名普通文件，留下一个装着 GBK 错误文本的残渣。
# 同测试与文档守卫，防的是一次 `git add .` 把它带进版本库。
/nul

# YueLiBot 运行时配置与数据（含 API Key、SQLite、日志和 Electron 缓存）
/config/
/data/

# 适配器的连接配置与插件同目录，但它带着协议端访问令牌和真实 QQ 号，
# 和 /config/ 同属运行时配置，不能随插件源码一起入库。
/adapters/*/config.toml
# 插件配置由宿主按插件声明的模型生成，属于运行时产物；
# 默认值的真相源是插件代码里的 config_model，不是这份文件。
/plugins/*/config.toml
/src/plugins/built_in/*/config.toml

# 任何位置的 SQLite 文件都不入库。/data/ 已经挡住绝大多数，但迁移链测试之类的
# 一次性副本会落在别处——2026-08-26 的 backups/.chain-test.v14.db 就是这么进去的，
# 一个 187 MB 的库文件让 .git 长到 234 MB。按文件类型再兜一层，不依赖目录约定。
*.db
*.db-shm
*.db-wal
*.sqlite3
/backups/

# 验收产物不入库。pytests/evidence/ 里是拿真机库跑出来的评估结果，
# jargon-name-guard.txt 直接列着 169 个真实人名（identities 与群名片），
# 公开仓库不能带。目录里的 .py 跑数工具已移到 scripts/eval/。
/pytests/evidence/
# uv 在测试目录留下的锁文件，随运行生成
/pytests/*.lock

# 给 agent 的规范约定归用户所有，只留在开发机工作区，不随代码分发。
# 同测试守卫，防的是一次 `git add .` 把它们带进版本库。
#
# 注意：docs/ 已不在此列。开发过程文档（方案、执行规格、复核修正单、抓取的
# 第三方参考资料）整体移到了仓库外的 ../YueLiBot-docs/；仓库里的 docs/ 从此
# 是随代码分发的用户文档，要入库。往 docs/ 里放过程文档会把它们公开出去。
/CLAUDE.md
/Agent.md

# 外部文档抓取脚本。产物落在仓库外的 ../YueLiBot-docs/reference/，脚本本身也只在
# 开发机上跑；同规范约定守卫，防的是一次 `git add .` 把它们带进版本库。
/scripts/fetch_docs.py
/scripts/fetch_reference_docs.mjs

# 只在本机跑的一次性量化读侧对照脚本：结论未采纳进项目，且它依赖的重放池与
# 生产候选池失配严重（逐位对账通过率不足百分之一），入库会把不可迁移的读数
# 当成历史基线；同规范约定守卫，防的是一次 `git add .` 把它带进版本库。
/scripts/eval/sq8_ab.py

# IDE
.idea/
.vscode/

# AI 工具的本地配置与缓存。除编辑器插件目录外，还包括代码审查工具的运行状态与
# 报告（.mimosa/），以及设计参考素材的抓取产物（.scrape/，含 png 等二进制）。
# 两者都是开发机本地产物，体积随运行次数增长，同 IDE 目录一并挡在版本库外。
.claude/
.cursor/
.aider*
.mimosa/
.scrape/

# 外部星月水母模型与 UI 参考素材，仅保留在本机工作区
/星月水母（新增麦克风游戏机）/
/星月水母UI/

# WebUI 性能探针的本地采样产物，随每次开发运行增长，属运行日志不入库。
/webui/logs/
