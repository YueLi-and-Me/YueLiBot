"""消息、事实、episode 和向量召回的记忆服务。

本包通过 SQLite 保存长期记忆，使用衰减、相似度和分词组件完成事实筛选，并向
对话服务提供统一的写入、历史和召回接口。
"""

# 事实抽取与主动自检共用同一个 meta 游标键；集中定义可防止一边改名后另一边
# 把全部历史消息误报成积压。
FACT_EXTRACT_CURSOR_KEY = 'fact_extract_cursor'
