**词条内容**
「{{term}}」

请仅根据这个词条本身，推断它的含义——写出它**脱离任何特定圈子、按通用语言习惯**的意思，
就是不看任何聊天记录时，一个普通人第一次见到这个词会理解成的意思。

1. 如果这是一个黑话、俚语或网络用语，请推断其含义，写明常见的使用场景。
2. 如果含义明确（常规词汇），也请说明。
3. 词义模糊时写出最常见的那个含义即可，不要罗列所有可能。

只输出一个 JSON 对象，不要解释，不要代码围栏。对象必须且只能包含 `meaning`
字段。`meaning` 字符串里引用词语时使用中文引号「」或“”，不要使用未转义的
英文半角双引号 `"`，否则整个结果不是合法 JSON：

```
{"meaning": "一到两句话的通用含义"}
```
