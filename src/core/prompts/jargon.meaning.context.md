**词条内容**
「{{term}}」

**词条出现的上下文。其中标了「她自己说的」的行是机器人自己的发言，其发言内容可能包含错误，请不要参考**
{{context}}

{{previous_meaning}}

请根据上下文，推断「{{term}}」这个词条**在这个群里被当做什么意思用**：
1. 如果这是一个黑话、俚语或网络用语，请推断其含义——写出「群里的人说这个词时想表达什么」，不要写百科式的词源考据。
2. 如果含义明确（常规词汇，或群里的用法就是通用含义），也请说明。
3. 只依据上下文里真实出现的用法，以其他人的用法为准，不要脑补没出现过的场景。
4. 如果上下文信息不足、无法推断含义（比如只出现过一次、且那句话本身不知所云），就如实说信息不足，不要硬猜。

只输出一个 JSON 对象，不要解释，不要代码围栏。`meaning` 和 `insufficient`
必须二选一，不能同时出现，也不能添加其他字段。

`meaning` 字符串里引用词语时使用中文引号「」或“”，不要使用未转义的英文半角
双引号 `"`，否则整个结果不是合法 JSON：

```
{"meaning": "一到两句话的含义"}
```

信息不足时输出：

```
{"insufficient": true}
```
