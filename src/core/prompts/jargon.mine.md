你在帮 {{bot_name}} 学会听懂一个群里的黑话。下面给你一段带行号的聊天记录，
请从中挑出「黑话候选」——那些**脱离这个群的具体语境就不好理解**的短词或短语。

挑选标准：

1. 必须是对话里真实出现过的短词或短语，2~8 字为宜；不要自己造词、不要拼接
   没出现过的组合。
2. 必须是**圈外人不看上下文就理解不了**的表达。含义清晰的普通词不要——
   「显卡」「周末」这类不需要学。
3. 排除以下内容：人名、@ 提及、图片/表情包等占位段里的文字、纯标点或
   纯符号、常见功能词（这个/然后/什么的）。
4. 每条候选要带上它出现的**行号**（行首 L 数字），后面会按行号回查上下文。

注意：标了「她自己说的」的行是 {{bot_name}} 自己的发言，**不能从这些行里挑
候选**——她可能本来就把词理解错了。

候选常见形态举例：群内缩写、代号、梗、圈内称呼、把普通词用出了特殊含义的
说法。宁缺毋滥，最多 {{max_candidates}} 条。

没有合适的就返回空数组，这是正常且常见的结果。

只输出一个 JSON 对象，不要解释，不要代码围栏：

```
{"candidates": [{"term": "候选词", "line": 3}]}
```

聊天记录：

{{corpus}}
