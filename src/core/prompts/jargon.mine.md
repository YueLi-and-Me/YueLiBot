{{corpus}}
请从上面这段聊天内容中提取「可能是黑话」的候选项（黑话/俚语/网络缩写/口头禅），帮 {{bot_name}} 学会听懂这个群。

提取规则：
1. 必须为对话中真实出现过的短词或短语，2~8 字为宜，尽量短小；不要自己造词、不要拼接没出现过的组合。
2. 必须是你无法理解含义、或者需要当前聊天圈内语境才能理解的词语；含义清晰的普通词语不要选——「显卡」「周末」这类不需要学。
3. 排除以下内容：人名、@ 提及、图片/表情包等占位段里的文字、纯标点或纯符号、常见功能词（这个/然后/什么的）。
4. 每条候选要带上它出现的行号（行首 L 数字），后面会按行号回查上下文。

黑话必须为以下几种类型：
- 由字母构成的汉语拼音首字母简写词，例如：nb、yyds、xswl
- 英文词语或产品的缩写，用英文字母概括一个词汇或含义，例如：CPU、FF14
- 中文词语的缩略，用几个汉字概括一个词汇或含义，例如：社死、内卷
- 圈内代号和梗、被赋予特殊含义的普通词

注意：标了「她自己说的」的行是 {{bot_name}} 自己的发言，不能从这些行里挑候选——她可能本来就把词理解错了。

宁缺毋滥，但请尽量提取所有可能的黑话，最多 {{max_candidates}} 条。没有合适的就返回空数组，这是正常且常见的结果。

只输出一个 JSON 对象，不要解释，不要代码围栏。对象必须且只能包含
`candidates` 字段，每条候选必须且只能包含 `term` 和 `line`：

```
{"candidates": [{"term": "候选词", "line": 3}]}
```
