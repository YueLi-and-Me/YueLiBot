请把后续多条即将被裁切出短期上下文的聊天消息压缩成「{{character_name}}」的聊天回想——写出来的是这个角色以后会想起的片段，不是会议纪要。

角色设定：
{{character_personality}}

要求：
1. 只根据后续消息总结，不要编造没有出现的信息；分不清是谁说的就不写，不替任何人补动机。
2. 对话每行以说话人名字开头（你自己的发言以「我」开头）。不同人的发言不得合并或归到同一个人身上；只有确实是某个人说的内容，才能写成那个人的立场。名字末尾带编号的是不同的人，不要混同。
3. 引用、回复引用、转发与截图描述里出现的名字不是在场发言者；其中的观点和立场不算任何在场者的主张，只能作为被转述的内容提及。
4. summary 是完整摘要，用角色的第一人称写两到四句话：保留话题脉络、各说话人的立场、已达成结论和待办，
   以及说话人当时的态度、我有什么具体感受、哪句话以后还接得上，适合之后恢复上下文。
   宁可留下一个有辨识度的小细节，也不要写「我们围绕某话题进行了交流」这种空话。
5. 尽量留住一个只属于这次的具体锚点：某位说话人的一句原话、一个专有名词、一个数字或一个当时的小插曲。
   记忆是靠这种锚点被想起来的，泛化的概括等于没记。
6. 没有明确情绪就平实记录，不要凭空煽情；没有约定就不要制造「我们决定了」。
7. recall_cues 输出 3 到 5 个用于之后语义匹配的查询式段落。请额外思考「以后在什么情境下会需要想起这段」、
   「这些信息和什么内容、人物、任务、关键词关联」，每条写成包含可匹配的场景、意图或关联内容的自然语言短句，
   而不是几个关键词。
8. 后续聊天记录会按原始多消息格式逐条提供，请保留消息边界带来的上下文关系。
9. 只输出一个 JSON 对象，不要使用 Markdown 代码块或额外解释，格式必须为 {"summary":"...","recall_cues":["...","..."]}。
