# 示例插件的配置。每个插件的开关都放在自己目录下的这份文件里：
# 装插件时它一起来，删插件时它一起走，主体配置里不会留下指向已删插件的孤儿项。
#
# 文件整个不存在时视为启用，所以只有想默认关闭、或者插件自己有配置项时才需要它。

[plugin]
# 示例默认关闭：它是教学素材，没有理由占用模型每一轮的工具声明预算。
# 想试就把这里改成 true，重启后 hello_yueli 工具就会出现在她的工具表里。
enabled = false
